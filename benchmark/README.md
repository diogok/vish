# Benchmark

Benchmarking is hard, and ideally should be case specific.

The benchmark here is just as a baseline, so know if things are getting faster, slower or if anything is going wrong.

This just compares to base, unmodified, nginx.

For performance, should use `SmpAllocator` and disable `logs`.
